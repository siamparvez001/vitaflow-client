import FundingClient from "./FundingClient";

export const metadata = {
    title: "Funding - VitaFlow",
    description: "Support VitaFlow with a donation",
};

export default function FundingPage() {
    return <FundingClient />;
}